// here is an array of strings
let dogArray: string[] = ["taco", "ceasar", "dogmeat"];
let numArray: number[] = [1, 2, 3, 4];
let movArray: object[] = [];

// function
// data types for params and args
// data types returned values

const getAge = (num: number): number => {
    return num * 4;
}

let result = getAge(34);
console.log(result);

